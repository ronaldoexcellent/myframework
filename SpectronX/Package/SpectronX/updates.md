# Source
replaceClass(targ, old_data, new_data)
replaceClasses(targ, array_of_old_data, array_of_new_data)

app.timer.clone(targ)
app.log(data)
app.state()
app.messages.show()
app.messages.hide()